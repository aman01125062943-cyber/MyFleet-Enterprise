
# مدير الأسطول - Enterprise SaaS

نظام إدارة وكالات السيارات متكامل (Front-end + Back-end).

## 🚀 خطوات التشغيل الفعلي (Go-Live)

### 1. إعداد قاعدة البيانات (Supabase)
1. قم بإنشاء مشروع جديد مجاني على [Supabase.com](https://supabase.com).
2. اذهب إلى قائمة **SQL Editor** في الشريط الجانبي.
3. انسخ محتوى الملف `full_db_setup.sql` الموجود في الكود بالكامل والصقه هناك.
4. اضغط على زر **Run** (سيقوم هذا بإنشاء الجداول والدوال المطلوبة).

### 2. ربط التطبيق بقاعدة بياناتك
1. في Supabase، اذهب إلى **Project Settings > API**.
2. ستجد مفتاحين: `Project URL` و `anon public key`.
3. قم بتحديث ملف `lib/supabaseClient.ts` بهذه البيانات، أو أضفها في إعدادات الاستضافة (Environment Variables) باسم:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`

### 3. التشغيل
- النظام جاهز الآن! أي مستخدم تقوم بإنشائه سيتم حفظه في قاعدة بياناتك الخاصة.

## 🛡️ مميزات الأمان المطبقة
- **Zero Trust Architecture:** التحقق يتم في كل عملية (Backend RPCs).
- **Data Isolation:** كل وكالة معزولة تماماً عن الأخرى باستخدام `org_id`.
- **Role Based Access:** الموظف لا يرى إلا ما يُسمح له به.
- **Secure Auth:** كلمات المرور والمصادقة تتم عبر دوال محمية (`SECURITY DEFINER`).

## 💼 الموديولات الجاهزة
1. **لوحة القيادة:** إحصائيات فورية للأرباح والمصاريف.
2. **المخزون:** إدارة السيارات وحالاتها.
3. **المالية:** تسجيل الإيرادات والمصروفات وحساب صافي الربح.
4. **الفريق:** إدارة الموظفين وصلاحياتهم الدقيقة.
5. **حاسبة الرحلات:** أداة للسائقين لحساب التكاليف.
